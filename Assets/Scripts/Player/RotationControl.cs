using UnityEngine;

namespace Player
{
    public class RotationControl : MonoBehaviour
    {
        // Update is called once per frame
        private void Update()
        {
            
        }
    }
}
